[package]
name = "launcher_market"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
url = "2"
tauri = { version = "2", features = [] }
launcher_download = { path = "../download" }
